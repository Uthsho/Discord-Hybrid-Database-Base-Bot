const CommandOption = require("./CommandOption");

class UserCommandOption extends CommandOption {
        
            constructor(main) {
                super(main);
    
                this.type = ''
            }
}